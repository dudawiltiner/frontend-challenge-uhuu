import { styled } from '@/../stitches.config';

export const NoDataText = styled('p', {
  fontFamily: '$roboto700',
  fontSize: '$small',
  lineHeight: '$xmedium',
  color: '$gray700',
  textAlign: 'center',
});
