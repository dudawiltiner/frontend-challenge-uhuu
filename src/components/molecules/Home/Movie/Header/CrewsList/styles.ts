import { styled } from '@/../stitches.config';

export const Container = styled('div', {
  maxWidth: '600px',
  marginTop: '16px',
  display: 'flex',
  flexWrap: 'wrap',
  justifyContent: 'left',
  alignItems: 'top',
  gap: '24px',
});
