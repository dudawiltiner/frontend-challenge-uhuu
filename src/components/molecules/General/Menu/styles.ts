import { styled } from '@/../stitches.config';

export const PinkContainer = styled('div', {
  paddingY: '16px',
  paddingLeft: '112px',
  height: 'max-content',
  backgroundColor: '$pink500',
  flexGrow: 1,
  justifyContent: 'left',
});
