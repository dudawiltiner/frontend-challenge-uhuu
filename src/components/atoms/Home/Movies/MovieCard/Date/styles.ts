import { styled } from '@/../stitches.config';

export const Text = styled('p', {
  fontFamily: '$roboto700',
  fontSize: '$xsmall',
  lineHeight: '$medium',
  textAlign: 'left',
  color: '$gray500',
});
