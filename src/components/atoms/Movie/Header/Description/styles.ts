import { styled } from '@/../stitches.config';

export const Text = styled('p', {
  fontFamily: '$roboto400',
  fontSize: '$xmedium',
  lineHeight: '$medium',
  textAlign: 'left',
  color: '$white',
});
